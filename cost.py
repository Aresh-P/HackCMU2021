def cost(user_requirements, proposed_solution):
    """
    user_requirements is of the form specified as the input of optimizer.
    proposed_solution is of the form specified as the output of optimizer.
    """
